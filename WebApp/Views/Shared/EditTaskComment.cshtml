@using WebApp.Models.ViewModels
@model TaskCommentViewModel

<h1>@ViewData["Title"] list</h1>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Instance.Id" />
    <input type="hidden" asp-for="ReturnUrl" />
    <input type="hidden" asp-for="Instance.TodoTaskId" />
    <input type="hidden" asp-for="Instance.Created" />

    <div class="mb-3">
        <label asp-for="Instance.Content" class="form-label"></label>
        <input asp-for="Instance.Content" class="form-control" />
        <span asp-validation-for="Instance.Content"></span>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Cancel"
       asp-route-returnUrl="@Model.ReturnUrl"
       class="btn btn-secondary">
        Cancel
    </a>
</form>